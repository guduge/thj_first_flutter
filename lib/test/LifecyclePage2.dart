import 'package:flutter/material.dart';
class LifecyclePage2  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: AppBar(
        title: Text("ssssss"),
      ),
      body: new Center(
        child: Text("xxxxxx"),
      ),
    );
  }
}
